

// difference between object and interface is that interface is template and obj is actual values
interface User {
	name : string,
	email: string,
	password:string;
}


const obj = {
	name:'San',
	email: 'san@gmail.com',
	password: 'Billie@124'
}

function getDataOfUser (obj: User){
	
}